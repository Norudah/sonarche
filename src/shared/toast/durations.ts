/** Toast durations by what missing the toast would cost. Countdowns pause on
 * hover, so these only matter for a reader looking elsewhere. */

/** A notice ("Added to X"); HeroUI's default. */
export const TOAST_GLANCE = 4_000;

/** Two lines: a failure with its reason, or a report with a caveat. */
export const TOAST_EXPLAINED = 8_000;

/** Carries an undo action. */
export const TOAST_UNDO = 10_000;

/** An offer that can be found again in Settings. */
export const TOAST_OFFER = 15_000;
