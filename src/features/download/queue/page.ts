/** A rendering budget: each card has animated markers. The backend serves
 * exactly this many per request. */
export const HISTORY_PAGE_SIZE = 25;

/** Finished downloads shown under the composer; the rest is in History. */
export const RECENT_JOBS = 5;
